Erro: cor de identificação não está sendo salva no pop-up de `pages/settings.tsx`, e o pop-up de `components/actives/activeForm.tsx` não está mostrando a paleta de cores certa, que é a mostrada no pop-up de `pages/settings.tsx`.